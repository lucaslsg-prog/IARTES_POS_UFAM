# Instalar PyTest e coverage através do 'pip install'

# Executar os seguintes comandos para visualizar cobertura de cenários:
    - coverage run --source=. -m pytest testCases.py 
    - coverage report --show-missing 